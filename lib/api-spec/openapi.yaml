openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: products
    description: Supplement products and blends
  - name: timing
    description: Timing map generation
  - name: leads
    description: Email capture
  - name: billing
    description: Subscription checkout
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /products:
    get:
      operationId: listProducts
      tags: [products]
      summary: List/search supplement products
      description: Returns products matching an optional search term, both standalone compounds and blends.
      parameters:
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Matching products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Product"
  /products/scan-label:
    post:
      operationId: scanProductLabel
      tags: [products]
      summary: Extract ingredients from a photo of a supplement label and save as a product
      description: Uses AI vision to read a supplement facts label photo and creates a new product with the extracted ingredients.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScanLabelInput"
      responses:
        "200":
          description: Product created from the scanned label
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
        "400":
          description: Invalid input or no ingredients could be read from the image
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /products/popular:
    get:
      operationId: listPopularProducts
      tags: [products]
      summary: List curated popular products for quick-add chips
      responses:
        "200":
          description: Popular products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Product"
  /timing-map:
    post:
      operationId: generateTimingMap
      tags: [timing]
      summary: Generate a timing map from a stack and daily anchors
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TimingMapInput"
      responses:
        "200":
          description: Generated timing map
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimingMap"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /leads:
    post:
      operationId: createLead
      tags: [leads]
      summary: Capture an email to unlock the full timing map
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LeadInput"
      responses:
        "201":
          description: Lead captured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Lead"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /checkout:
    post:
      operationId: createCheckoutSession
      tags: [billing]
      summary: Start a Stripe checkout session for the Pro plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckoutInput"
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutSession"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /checkout/verify:
    get:
      operationId: verifyCheckoutSession
      tags: [billing]
      summary: Verify a completed Stripe checkout session
      parameters:
        - name: sessionId
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutVerification"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
    Ingredient:
      type: object
      required: [name, mgAmount]
      properties:
        name:
          type: string
        mgAmount:
          type: number
    Product:
      type: object
      required: [id, name, type, ingredients]
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
          enum: [standalone, blend]
        badge:
          type: ["string", "null"]
        ingredients:
          type: array
          items:
            $ref: "#/components/schemas/Ingredient"
    ScanLabelInput:
      type: object
      required: [imageBase64, mimeType]
      properties:
        imageBase64:
          type: string
          description: Base64-encoded (no data URL prefix) photo of a supplement facts label.
        mimeType:
          type: string
          description: "Image MIME type, e.g. image/jpeg or image/png."
        productNameHint:
          type: ["string", "null"]
          description: Optional product name if visible/known, used to seed the created product's name.
    Anchors:
      type: object
      required: [wake, breakfast, dinner, bed]
      properties:
        wake:
          type: string
          description: "24h time, HH:MM"
        breakfast:
          type: string
          description: "24h time, HH:MM"
        dinner:
          type: string
          description: "24h time, HH:MM"
        bed:
          type: string
          description: "24h time, HH:MM"
        medicationName:
          type: ["string", "null"]
        medicationTime:
          type: ["string", "null"]
          description: "24h time, HH:MM"
        coffeeTime:
          type: ["string", "null"]
          description: "24h time, HH:MM"
    TimingMapInput:
      type: object
      required: [productIds, anchors]
      properties:
        productIds:
          type: array
          items:
            type: string
        anchors:
          $ref: "#/components/schemas/Anchors"
    TimingPill:
      type: object
      required: [label, isAnchor]
      properties:
        label:
          type: string
        isAnchor:
          type: boolean
        reason:
          type: ["string", "null"]
        source:
          type: ["string", "null"]
    TimingSlot:
      type: object
      required: [time, context, pills]
      properties:
        time:
          type: string
          description: "24h time, HH:MM"
        context:
          type: string
        note:
          type: ["string", "null"]
        pills:
          type: array
          items:
            $ref: "#/components/schemas/TimingPill"
    AuditItem:
      type: object
      required: [ingredientName, totalMg, products]
      properties:
        ingredientName:
          type: string
        totalMg:
          type: number
        products:
          type: array
          items:
            type: string
    TimingMap:
      type: object
      required: [slots, audit]
      properties:
        slots:
          type: array
          items:
            $ref: "#/components/schemas/TimingSlot"
        audit:
          type: array
          items:
            $ref: "#/components/schemas/AuditItem"
    LeadInput:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    Lead:
      type: object
      required: [id, email, createdAt]
      properties:
        id:
          type: string
        email:
          type: string
        createdAt:
          type: string
    CheckoutInput:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    CheckoutSession:
      type: object
      required: [url]
      properties:
        url:
          type: ["string", "null"]
    CheckoutVerification:
      type: object
      required: [active]
      properties:
        active:
          type: boolean
