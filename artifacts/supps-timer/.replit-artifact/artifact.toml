kind = "web"
previewPath = "/"
title = "Supps Timer"
version = "1.0.0"
id = "artifacts/supps-timer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22232

[services.development]
run = "pnpm --filter @workspace/supps-timer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/supps-timer", "run", "build" ]
publicDir = "artifacts/supps-timer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22232"
BASE_PATH = "/"
